<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rekentuinen">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".newOefenTafel"></activity>
        <activity android:name=".newOefenen" />
        <activity android:name=".newToetsTafel" />
        <activity android:name=".newTafels" />
        <activity android:name=".toetsTafel" />
        <activity android:name=".showTafel" />
        <activity android:name=".inzienActivity" />
        <activity
            android:name=".oefensommenActivity"
            android:parentActivityName=".oefenen" />
        <activity android:name=".toetsen" />
        <activity android:name=".oefenen" />
        <activity android:name=".MainActivity" />
        <activity android:name=".welkomAcitvity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>